# 🍵 TeaBot v3.0

Telegram-бот для пересылки сообщений из закрытой группы в публичный канал по команде `/tea`.

## 📋 Возможности

- ✅ **Кастомная основная команда** - настраивается через `MAIN_COMMAND` (по умолчанию `/tea`)
- ✅ **Гибкие настройки доступа** - каждая команда может быть админской или публичной
- ✅ **Управление логированием** - можно отключить через `ENABLE_LOGGING`
- ✅ **Три варианта анонсов:**
  - Простой текстовый анонс
  - Фото с подписью
  - Фото с кастомным текстом
- ✅ **Команда цитат** - `/quot` для публикации случайных цитат из файла
- ✅ Поддержка видео и видео-заметок
- ✅ Суточный лимит пересылок (настраивается)
- ✅ Автоматический сброс в настраиваемое время
- ✅ Система банов пользователей
- ✅ Статистика и топ пользователей
- ✅ База данных SQLite для учёта пересылок
- ✅ Docker-контейнер для простого развёртывания

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd Tea_bot
```

### 2. Настройка переменных окружения

Скопируйте `env.example` в `.env` и заполните значения:

```bash
cp env.example .env
nano .env
```

**Обязательные параметры:**
- `BOT_TOKEN` - токен от [@BotFather](https://t.me/BotFather)
- `GROUP_ID` - ID группы (начинается с `-100`)
- `CHANNEL_ID` - ID канала (начинается с `-100`)
- `ADMINS` - ID администраторов через запятую

**Дополнительные настройки:**
- `ENABLE_LOGGING` - включить/отключить логирование (true/false)
- `MAIN_COMMAND` - основная команда (по умолчанию "tea")
- `BAN_ADMIN_ONLY` - доступ к /ban (true/false)
- `UNBAN_ADMIN_ONLY` - доступ к /unban (true/false)
- `STATS_ADMIN_ONLY` - доступ к /stats (true/false)
- `RESET_ADMIN_ONLY` - доступ к /reset (true/false)

#### 🔍 Как узнать ID группы/канала?

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в группу/канал
2. Скопируйте ID из сообщения бота
3. Для групп/каналов ID начинается с `-100`

### 3. Запуск через Docker (рекомендуется)

#### Первый запуск

```bash
# Переход в директорию docker
cd docker

# Запуск контейнера в фоновом режиме
docker-compose up -d

# Просмотр логов для проверки запуска
docker-compose logs -f teabot
```

#### Пересборка после изменений кода

```bash
cd docker

# Остановка контейнера
docker-compose down

# Пересборка образа без кэша и запуск
docker-compose up -d --build --force-recreate

# Или полная пересборка с очисткой volumes (если нужно)
docker-compose down -v
docker-compose up -d --build
```

#### Управление контейнером

```bash
cd docker

# Просмотр статуса
docker-compose ps

# Просмотр логов в реальном времени
docker-compose logs -f teabot

# Просмотр последних 100 строк логов
docker-compose logs --tail=100 teabot

# Остановка контейнера
docker-compose stop

# Запуск остановленного контейнера
docker-compose start

# Полная остановка и удаление
docker-compose down

# Остановка с удалением volumes (удалит БД!)
docker-compose down -v
```

#### Проверка работоспособности

После запуска проверьте логи на наличие ошибок:

```bash
cd docker
docker-compose logs teabot | grep -i "error\|started\|ready"
```

Успешный запуск должен показать:
```
INFO: Starting TeaBot v3.0...
INFO: Bot started successfully! Polling...
```

### 4. Запуск без Docker

#### Установка зависимостей

```bash
# Создание виртуального окружения (рекомендуется)
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

#### Запуск бота

```bash
# Убедитесь, что виртуальное окружение активировано
# Запуск из корня проекта
python3 bot.py
```

#### Запуск в фоновом режиме (Linux/Mac)

```bash
# Запуск в фоне с перенаправлением вывода
nohup python3 bot.py > logs/bot_output.log 2>&1 &

# Просмотр процесса
ps aux | grep bot.py

# Остановка (найдите PID и выполните)
kill <PID>
```

#### Проверка работоспособности

После запуска проверьте:
1. **Логи** - должны быть сообщения о успешном запуске
2. **База данных** - файл `data/forwards.db` должен создаться автоматически
3. **Тест команды** - отправьте `/help` боту в группе

### 5. Проверка конфигурации перед запуском

Перед первым запуском убедитесь:

```bash
# Проверка наличия .env файла
ls -la .env

# Проверка обязательных переменных (Linux/Mac)
grep -E "BOT_TOKEN|GROUP_ID|CHANNEL_ID|ADMINS" .env

# Проверка структуры директорий
ls -la data/ logs/
```

**Важно:**
- ✅ Файл `.env` должен существовать и содержать все обязательные параметры
- ✅ Директории `data/` и `logs/` должны существовать (создаются автоматически)
- ✅ Бот должен быть добавлен в группу и канал с правами администратора
- ✅ Файл `quotation.txt` должен существовать для работы команды `/quot`

## 📖 Использование

### Команды бота

#### `/{MAIN_COMMAND}` - Основной анонс
Команда настраивается через `MAIN_COMMAND` (по умолчанию `/tea`):

**Простой анонс:**
```
/tea
```
→ В канале: `Чай by @username`

**Фото с подписью:**
```
/tea
[фото]
```
→ В канале: фото с подписью `Чай by @username`

**Фото с кастомным текстом:**
```
/tea Приглашаю на встречу!
[фото]
```
→ В канале: фото с подписью `Чай. "Приглашаю на встречу!" by @username`

#### `/quot` - Случайная цитата
Публикует случайную цитату из файла `quotation.txt`:

**Текстовая цитата:**
```
/quot
```
→ В канале: случайная цитата с подписью `by @username`

**Фото с цитатой:**
```
/quot
[фото]
```
→ В канале: фото с цитатой в подписи

#### `/help` - Справка
Показывает список команд и информацию о боте.

#### `/stats` - Статистика
Показывает количество пересылок за сегодня и оставшийся лимит.
**Доступ:** настраивается через `STATS_ADMIN_ONLY`

#### `/reset` - Сброс лимита
Принудительно сбрасывает счётчик пересылок на сегодня.
**Доступ:** настраивается через `RESET_ADMIN_ONLY`

#### `/ban` - Запрет на анонсы
Забирает возможность использовать основную команду.
**Доступ:** настраивается через `BAN_ADMIN_ONLY`

Формат: `/ban @username часы [причина]`
```
/ban @user123 24 Спам
```

#### `/unban` - Снятие запрета
Снимает бан с пользователя.
**Доступ:** настраивается через `UNBAN_ADMIN_ONLY`

Формат: `/unban @username` или ответ на сообщение

## 🏗️ Структура проекта

```
Tea_bot/
├── bot.py                      # 🚀 Точка входа приложения
│
├── src/                        # 📦 Исходный код
│   ├── __init__.py
│   ├── config.py               # ⚙️ Конфигурация
│   ├── logger.py               # 📝 Настройка логирования
│   │
│   ├── handlers/               # 🎯 Обработчики команд
│   │   ├── __init__.py
│   │   └── commands.py         # Обработчики /tea, /quot, /help, /stats, /reset, /ban, /unban
│   │
│   ├── database/               # 💾 Работа с базой данных
│   │   ├── __init__.py
│   │   └── repository.py       # Repository паттерн для БД
│   │
│   └── utils/                  # 🛠️ Вспомогательные функции
│       ├── __init__.py
│       └── helpers.py          # Функции форматирования, валидации и работы с цитатами
│
├── docker/                     # 🐳 Docker конфигурация
│   ├── Dockerfile
│   └── docker-compose.yml
│
├── data/                       # 💾 База данных SQLite
│   └── forwards.db
│
├── logs/                       # 📋 Логи бота
│   └── teabot.log
│
├── quotation.txt               # 📜 Файл с цитатами для команды /quot
├── requirements.txt            # 📚 Python зависимости
├── env.example                 # 📋 Пример конфигурации
├── .gitignore                  # 🚫 Игнорируемые файлы
└── README.md                   # 📖 Документация
```

## 🔧 Технические детали

### Архитектура
- **Язык:** Python 3.11+
- **Фреймворк:** aiogram 3.x
- **База данных:** SQLite
- **Принципы:** Структурное программирование, SOLID

### Модульная организация

#### 📦 **src/config.py**
Управление конфигурацией через переменные окружения. Валидация настроек при запуске.

#### 📝 **src/logger.py**
Централизованная система логирования с записью в файл и вывод в консоль.

#### 🎯 **src/handlers/**
Обработчики команд бота с проверкой прав доступа и лимитов.

#### 💾 **src/database/**
Repository паттерн для работы с SQLite. Инкапсулирует всю логику работы с БД.

#### 🛠️ **src/utils/**
Вспомогательные функции для форматирования сообщений и получения информации о пользователях.

### База данных

Таблица `forwards`:
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | ID записи |
| `username` | TEXT | Имя пользователя |
| `message_type` | TEXT | Тип (text/photo/video/video_note) |
| `datetime` | TIMESTAMP | Время пересылки |

### Лимиты

- **Суточный лимит:** 5 пересылок (настраивается в `.env`)
- **Сброс:** Каждый день в 04:00 МСК (настраивается)
- **Подсчёт:** По записям в БД с timestamp >= 04:00 текущих суток

## 📝 Логирование

Все действия записываются в `logs/teabot.log`:
- ✅ Успешные пересылки
- ⚠️ Превышение лимита
- ❌ Ошибки API

Пример:
```
[2025-10-13 12:45:22] INFO: Sent photo tea by @username
[2025-10-13 14:20:00] WARNING: Limit reached for @username
[2025-10-13 15:00:00] ERROR: Failed to send message — TelegramAPIError
```

## 🐳 Docker

### Команды управления

```bash
# Из директории docker/
cd docker

# Пересборка после изменений
docker-compose up -d --build

# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f teabot

# Остановка
docker-compose down

# Полная очистка (с удалением volumes)
docker-compose down -v
```

### Healthcheck

Бот включает healthcheck, который каждые 60 секунд проверяет доступность базы данных.

Проверка статуса healthcheck:
```bash
cd docker
docker inspect teabot | grep -A 10 Health
```

### Мониторинг и обслуживание

#### Регулярные задачи

1. **Проверка логов** - рекомендуется проверять логи раз в день:
   ```bash
   docker-compose logs --tail=50 teabot
   ```

2. **Проверка размера БД** - следите за размером базы данных:
   ```bash
   ls -lh data/forwards.db
   ```

3. **Очистка старых логов** (опционально):
   ```bash
   # Ротация логов (если настроена)
   # Или ручная очистка
   > logs/teabot.log  # Очистить файл логов
   ```

4. **Обновление бота** - при обновлении кода:
   ```bash
   cd docker
   git pull  # Из корня проекта
   docker-compose down
   docker-compose up -d --build --force-recreate
   ```

#### Автозапуск при перезагрузке сервера

Для автоматического запуска бота при перезагрузке системы:

**Docker (рекомендуется):**
```bash
cd docker
docker-compose up -d
# Docker Compose автоматически запустит контейнеры при перезагрузке
```

**Systemd (для запуска без Docker):**
Создайте файл `/etc/systemd/system/teabot.service`:
```ini
[Unit]
Description=TeaBot Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/Tea_bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable teabot
sudo systemctl start teabot
sudo systemctl status teabot
```

## 🛠️ Разработка

### Локальный запуск для тестирования

```bash
# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt

# Запуск из корня проекта
python bot.py
```

### Структура импортов

Все импорты используют относительные пути от `src`:
```python
from src.config import config
from src.logger import logger
from src.handlers import router
from src.database.repository import db_repo
from src.utils.helpers import get_user_display_name
```

### SOLID принципы в коде

✅ **Single Responsibility** - каждый модуль отвечает за одну задачу:
  - `config.py` - только конфигурация
  - `logger.py` - только логирование
  - `repository.py` - только работа с БД

✅ **Open/Closed** - легко расширяется через добавление новых handlers или helpers

✅ **Liskov Substitution** - использование Repository pattern для работы с БД

✅ **Interface Segregation** - минимальные зависимости между модулями

✅ **Dependency Inversion** - зависимость от абстракций (config, logger, db_repo)

## 📄 Лицензия

MIT

## 🤝 Поддержка

### Проверка работоспособности

#### Через Docker

```bash
cd docker

# Проверка статуса контейнера
docker-compose ps

# Просмотр логов
docker-compose logs -f teabot

# Проверка healthcheck
docker-compose ps | grep teabot
```

#### Без Docker

```bash
# Проверка процесса
ps aux | grep bot.py  # Linux/Mac
tasklist | findstr bot.py  # Windows

# Просмотр логов
tail -f logs/teabot.log  # Linux/Mac
Get-Content logs/teabot.log -Wait  # Windows PowerShell
```

### Типичные проблемы и решения

#### ❌ Бот не запускается

**Проблема:** Ошибка при запуске контейнера или процесса

**Решение:**
1. Проверьте логи: `docker-compose logs teabot` или `tail -f logs/teabot.log`
2. Убедитесь, что все переменные в `.env` заполнены корректно
3. Проверьте формат ID группы/канала (должны начинаться с `-100`)
4. Убедитесь, что токен бота валидный

#### ❌ Ошибка "Configuration validation failed"

**Проблема:** Бот не может загрузить конфигурацию

**Решение:**
1. Проверьте наличие файла `.env` в корне проекта
2. Убедитесь, что все обязательные переменные заполнены:
   - `BOT_TOKEN`
   - `GROUP_ID`
   - `CHANNEL_ID`
   - `ADMINS`
3. Проверьте синтаксис `.env` файла (нет ли лишних пробелов, кавычек)

#### ❌ Бот не отвечает на команды

**Проблема:** Команды не работают в группе

**Решение:**
1. Убедитесь, что бот добавлен в группу с правами администратора
2. Проверьте, что `GROUP_ID` в `.env` соответствует ID вашей группы
3. Убедитесь, что бот запущен и работает (проверьте логи)
4. Проверьте, что команда отправляется в правильной группе

#### ❌ Ошибка при отправке в канал

**Проблема:** "Failed to send message to channel"

**Решение:**
1. Убедитесь, что бот добавлен в канал с правами администратора
2. Проверьте, что `CHANNEL_ID` в `.env` соответствует ID вашего канала
3. Убедитесь, что бот имеет права на публикацию сообщений в канале

#### ❌ База данных не создаётся

**Проблема:** Ошибки при работе с БД

**Решение:**
1. Проверьте права на запись в директорию `data/`
2. Убедитесь, что директория `data/` существует
3. Для Docker: проверьте, что volume смонтирован корректно

#### ❌ Команда /quot не работает

**Проблема:** Цитаты не загружаются

**Решение:**
1. Убедитесь, что файл `quotation.txt` существует в корне проекта
2. Проверьте формат файла (одна цитата на строку)
3. Для Docker: убедитесь, что файл скопирован в образ (проверьте Dockerfile)

#### ❌ Контейнер постоянно перезапускается

**Проблема:** Docker контейнер в статусе "Restarting"

**Решение:**
1. Просмотрите логи: `docker-compose logs teabot`
2. Проверьте healthcheck: возможно, проблема с доступом к БД
3. Проверьте переменные окружения в `.env`
4. Попробуйте пересобрать контейнер: `docker-compose up -d --build --force-recreate`

### Получение помощи

При возникновении проблем:
1. **Проверьте логи:** `docker-compose logs -f` (из директории docker/) или `tail -f logs/teabot.log`
2. **Проверьте `.env` файл:** все обязательные переменные должны быть заполнены
3. **Убедитесь, что бот добавлен в группу и канал с правами админа**
4. **Проверьте версию Python:** требуется Python 3.11+
5. **Проверьте версию Docker:** рекомендуется Docker 20.10+

---

**TeaBot v3.0** — простой и надёжный бот для ваших анонсов! 🍵
